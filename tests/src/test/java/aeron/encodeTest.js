function encodeTest(data) {
    console.log("Data received for testing:", data);
    const encodedData = new Uint8Array([10, 20, 30, 40, 50]);
    console.log("Encoded test data:", encodedData);
    return encodedData;
}
