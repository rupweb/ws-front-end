application.name=ws-front-end-app-test

# Development environment requires to import additional properties to run locally
config.import=application-dev.properties

# Remote build environment properties path
# config.import=application-remote.properties
